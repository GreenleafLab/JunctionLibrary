import pandas as pd
df = pd.DataFrame( columns = ['Sequence', 'Variant Number', 'Barcode', 'Junction Topology','Loop', 'Receptor',  'Junction Sequence', 'JunctionPos', 'HelixContext',
... 'HelixLength1', 'Helixlength2', 'Totallength', 'HelixSequence'])                            